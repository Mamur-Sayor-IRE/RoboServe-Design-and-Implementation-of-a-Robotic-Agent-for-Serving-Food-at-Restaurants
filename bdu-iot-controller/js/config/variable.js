var log = console.log;
var domain = "https://192.168.32.22/bdu-iot-controller";
var $SelectedText = "";
var $SelectedTextStartIndex = "";
var $SelectedTextEndIndex = "";

// if (!GetCookie("u_id")) SetCookie("u_id", "", 365);
// if (!GetCookie("u_name")) SetCookie("u_name", "", 365);
// if (!GetCookie("u_email")) SetCookie("u_email", "", 365);
// if (!GetCookie("u_img")) SetCookie("u_img", "", 365);
